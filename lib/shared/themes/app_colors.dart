import 'dart:ui';

class AppColors {
  static const Color pacificCyan = Color.fromRGBO(1, 150, 170, 1);  //Select NavBar menu
  static const Color black = Color.fromRGBO(0, 0, 0, 1); // Background button, NavBar menu
  static const Color white = Color.fromRGBO(255, 255, 255, 1); //Background app and text color
}